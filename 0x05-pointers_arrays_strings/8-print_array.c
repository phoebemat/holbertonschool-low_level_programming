#include "holberton.h"
#include <stdio.h>
/**
 * print_array - print n elements of array
 * @n: number of elementss
 * @a: pointer to n
 * Return: Always 0.
 */
void print_array(int *a, int n)
{
	int x;

	for (x = n; x <= n; x++)
	{
	printf("%d", a[n]);
	printf(", ");
	printf("\n");
	}
}
