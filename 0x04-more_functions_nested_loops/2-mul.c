#include "holberton.h"

/**
 * mul - function that multiplies a and b
 * @a: first integer multiplied
 * @b: second integer multiplied
 * Return: Always 0 (Success)
 */

int mul(int a, int b)
	{
	int result = a * b;

	return (result);
	}
